# MACRO-DATA 파이프라인 의존성
# Python 3.11 기준 검증
# 설치: pip install -r requirements.txt

# 데이터 처리
pandas==2.2.3
numpy==1.26.4

# Excel 출력
xlsxwriter==3.2.0    # build_excel_dashboard_plus.py (engine='xlsxwriter')
openpyxl==3.1.5      # postprocess_add_overview_links.py (load_workbook)

# 주가 데이터 수집
finance-datareader==0.9.93   # fetch_prices_fdr.py, fetch_prices_fmp.py (FDR fallback)

# Parquet I/O (L1 마이그레이션 — utils_io.py, USE_PARQUET=1 환경변수로 활성화)
pyarrow>=15.0.0              # utils_io.py read_df/write_df Parquet 백엔드
